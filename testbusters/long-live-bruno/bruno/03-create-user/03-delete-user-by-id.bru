meta {
  name: 03-delete-user-by-id
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/users/{{CURR_USER_ID}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
